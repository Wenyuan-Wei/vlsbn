name: vlsbn
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - matplotlib
  - seaborn
  - statsmodels
  - joblib
  - multiprocess
  - cython
  - pyarrow
  - pytest
  - conda-forge::rdkit        # pip rdkit segfaults on many HPC Linux nodes
  - pip
  - pip:
      - biopython
      - requests
      - vmd-python
